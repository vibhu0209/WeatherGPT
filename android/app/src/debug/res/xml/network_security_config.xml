<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. This file is in src/debug and is never part of a release APK, which keeps
  using the HTTPS-only policy in src/main.

  Cleartext is permitted so the app can reach a developer's local FastAPI backend over http://:
    - 10.0.2.2 is the host loopback as seen from the standard Android emulator
    - a physical phone uses the PC's LAN address, which differs per machine and cannot be
      listed here, so the debug default has to allow cleartext rather than an allowlist

  The emulator and loopback hosts are still called out explicitly below so the intent is
  readable and so they keep working if someone later tightens base-config to false.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
